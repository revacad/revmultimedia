# Paystack (application fee + portal invoices)
# Get keys from https://dashboard.paystack.com → Settings → API Keys & Webhooks
# Use test keys (pk_test_ / sk_test_) in development.

NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=
PAYSTACK_SECRET_KEY=

# Webhook URL (production): https://your-domain.com/api/paystack/webhook
# After payment, the portal also calls POST /api/paystack/verify (no extra env needed).
